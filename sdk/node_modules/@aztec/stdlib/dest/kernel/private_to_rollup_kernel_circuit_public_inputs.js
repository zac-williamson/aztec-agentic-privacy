import { DomainSeparator, PRIVATE_TO_ROLLUP_KERNEL_CIRCUIT_PUBLIC_INPUTS_LENGTH } from '@aztec/constants';
import { poseidon2HashWithSeparator } from '@aztec/foundation/crypto/poseidon';
import { bufferSchemaFor } from '@aztec/foundation/schemas';
import { BufferReader, bigintToUInt64BE, serializeToBuffer, serializeToFields } from '@aztec/foundation/serialize';
import { bufferToHex, hexToBuffer } from '@aztec/foundation/string';
import { AztecAddress } from '../aztec-address/index.js';
import { Gas } from '../gas/gas.js';
import { TxConstantData } from '../tx/tx_constant_data.js';
import { PrivateToRollupAccumulatedData } from './private_to_rollup_accumulated_data.js';
/**
 * Outputs from the public kernel circuits.
 * All Public kernels use this shape for outputs.
 */ export class PrivateToRollupKernelCircuitPublicInputs {
    constants;
    end;
    gasUsed;
    feePayer;
    expirationTimestamp;
    constructor(/**
     * Data which is not modified by the circuits.
     */ constants, /**
     * Data accumulated from both public and private circuits.
     */ end, /**
     * Gas used during this transaction
     */ gasUsed, /**
     * The address of the fee payer for the transaction.
     */ feePayer, /**
     * The timestamp by which the transaction must be included in a block.
     */ expirationTimestamp){
        this.constants = constants;
        this.end = end;
        this.gasUsed = gasUsed;
        this.feePayer = feePayer;
        this.expirationTimestamp = expirationTimestamp;
    }
    getNonEmptyNullifiers() {
        return this.end.nullifiers.filter((n)=>!n.isZero());
    }
    toBuffer() {
        return serializeToBuffer(this.constants, this.end, this.gasUsed, this.feePayer, bigintToUInt64BE(this.expirationTimestamp));
    }
    /**
   * Deserializes from a buffer or reader, corresponding to a write in cpp.
   * @param buffer - Buffer or reader to read from.
   * @returns A new instance of PrivateToRollupKernelCircuitPublicInputs.
   */ static fromBuffer(buffer) {
        const reader = BufferReader.asReader(buffer);
        return new PrivateToRollupKernelCircuitPublicInputs(reader.readObject(TxConstantData), reader.readObject(PrivateToRollupAccumulatedData), reader.readObject(Gas), reader.readObject(AztecAddress), reader.readUInt64());
    }
    static empty() {
        return new PrivateToRollupKernelCircuitPublicInputs(TxConstantData.empty(), PrivateToRollupAccumulatedData.empty(), Gas.empty(), AztecAddress.ZERO, 0n);
    }
    toString() {
        return bufferToHex(this.toBuffer());
    }
    static fromString(str) {
        return PrivateToRollupKernelCircuitPublicInputs.fromBuffer(hexToBuffer(str));
    }
    /** Returns a hex representation for JSON serialization. */ toJSON() {
        return this.toBuffer();
    }
    static getFields(fields) {
        return [
            fields.constants,
            fields.end,
            fields.gasUsed,
            fields.feePayer,
            fields.expirationTimestamp
        ];
    }
    /** Creates an instance from a hex string. */ static get schema() {
        return bufferSchemaFor(PrivateToRollupKernelCircuitPublicInputs);
    }
    toFields() {
        const fields = serializeToFields(...PrivateToRollupKernelCircuitPublicInputs.getFields(this));
        if (fields.length !== PRIVATE_TO_ROLLUP_KERNEL_CIRCUIT_PUBLIC_INPUTS_LENGTH) {
            throw new Error(`Invalid number of fields for PrivateToRollupKernelCircuitPublicInputs. Expected ${PRIVATE_TO_ROLLUP_KERNEL_CIRCUIT_PUBLIC_INPUTS_LENGTH}, got ${fields.length}`);
        }
        return fields;
    }
    hash() {
        return poseidon2HashWithSeparator(this.toFields(), DomainSeparator.PRIVATE_TX_HASH);
    }
}
