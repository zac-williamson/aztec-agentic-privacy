import { z } from 'zod';
import { schemas, zodFor } from '../schemas/index.js';
export const AllowedElementSchema = zodFor()(z.union([
    z.object({
        address: schemas.AztecAddress,
        selector: schemas.FunctionSelector
    }),
    z.object({
        address: schemas.AztecAddress
    }),
    z.object({
        classId: schemas.Fr,
        selector: schemas.FunctionSelector
    }),
    z.object({
        classId: schemas.Fr
    })
]));
